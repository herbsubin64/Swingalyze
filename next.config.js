/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Leave output undefined so Vercel serves the Next server build correctly.
  // Do NOT set `output: 'export'` unless you intend to use static export.
};

module.exports = nextConfig;
